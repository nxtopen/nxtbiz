import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#24794d',
    },
    secondary: {
      main: '#7fbb5c',
    },
  },
});

export default theme;